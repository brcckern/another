<!DOCTYPE html>
<html lang="tr">
<head>
    <%- include('../partials/head', title) %>
</head>
<body style="background-color: #f7f7f7">
    <%- include('../partials/nav') %>
    <div class="container mt-3">
        <div class="row">
            <div class="col-md-12">
                <% if (durum != "undefined" && durum == "gonderildi") { %>
                    <div class="alert alert-success">İletişim formunuz başarıyla gönderildi! En kısa sürede sizinle iletişime geçeceğiz.</div>
                <% } %>
                <h3 class="h3 mb-3"><%= title %></h3>
                <%- include('../partials/contact-content') %>
            </div>
        </div>
        <%- include('../partials/footer') %>
    </div>
</body>
</html>