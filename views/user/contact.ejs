<!DOCTYPE html>
<html lang="tr">
<head>
    <%- include('../partials/head', title) %>
</head>
<body style="background-color: #f7f7f7">
    <%- include('../partials/nav') %>
    <div class="container mt-3">
        <div class="row">
            <div class="col-md-3">
                <%- include('../partials/lessons', {lessons: lessons}) %>
            </div>
            <div class="col-md-9">
                <% if (durum != "undefined" && durum == "gonderildi") { %>
                    <div class="alert alert-success">İletişim formunuz başarıyla gönderildi!</div>
                <% } %>
                <h3 class="h3 mb-3"><%= title %></h3>
                <%- include('../partials/contact-content') %>
            </div>
        </div>
        <%- include('../partials/footer') %>
    </div>
</body>
</html>