<div class="list-group mb-3">
    <a href="/" class="list-group-item list-group-item-action">Tüm Dersler</a>
    <% lessons.forEach(lesson => { %>
        <a href="/dersler/<%= lesson.id %>" class="list-group-item list-group-item-action <%= selectedIndex == lesson.id ? "active" : "" %>"><%= lesson.name %></a>
    <% }) %>
</div>